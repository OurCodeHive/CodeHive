import React from "react";
import { useRecoilValue } from "recoil";
import { userState } from '@/atom/UserAtom';

function StudyGroupUserList(props:any){
    const param = {
        page: 0,
        size: 10
    }
}

export default StudyGroupUserList;
